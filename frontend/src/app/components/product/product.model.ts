export interface Product { //esse interface é um tipo de recurso que só está presente no typeScript
  // Nessa parte é definida os atributos da interface da aplicação
  id?: number
  name: string
  price: number
}